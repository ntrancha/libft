<?              /*CODE NK*/

    new tabstr tableau = ["23","12","42","32"];
    new string name_variable = "test42";
    new string test42 = "ok les lapins";
    new int coc = 42;

    if (coc == 42)
    {
        test42.replace(lapins, LAPINOS);
    }
    if (coc > 40 + 1 && coc < 50 - 5 && (code == 412 / 2 || name_variable == "test42"))
    {
        ${name_variable}.put();
    }
    test2 = test;
    test2.convert(mem);
    LEN = test.len();
    test.tolower();
    test.replace(les, los);
    test.toupper();
    #INFOS;

    #DBG_PROG;
?>
